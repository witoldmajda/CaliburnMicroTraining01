<Window x:Class="CaliburnMicroTraining01.Views._02ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CaliburnMicroTraining01.Views"
        mc:Ignorable="d"
        Title="_02ShellView" Height="450" Width="800">
    <DockPanel>
        <TabControl x:Name="Items">
            <TabItem Header="PageOneView">
                <TextBlock Text="{Binding DisplayName}" />
            </TabItem>
            <TabItem Header="PageTwoView">

            </TabItem>
            <!--<TabControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding DisplayName}" />
                    </StackPanel>
                </DataTemplate>
            </TabControl.ItemTemplate>-->
        </TabControl>
    </DockPanel>
</Window>
